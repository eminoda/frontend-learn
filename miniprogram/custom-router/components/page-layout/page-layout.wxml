<view>
  <!-- <button bind:tap="toNext">跳转页面{{pageNumber+1}}</button> -->
  <view>当前页面：{{page.route}}</view>
  <view>
    当前微信栈：
    <view wx:for="{{wxRouterStack}}" wx:key="index">{{item}}</view>
  </view>
  <button type="primary" disabled="{{wxRouterStack.length>=10}}" bindtap="toNext">跳转下一页面</button>
</view>