<view>
    <view>
        当前第{{pageNumber}}个页面
    </view>
    <view>
        <button bind:tap="toNext">跳转页面{{pageNumber+1}}</button>

        <button bind:tap="toWebview">跳转 webview</button>

        <button bind:tap="toBack">返回历史页面</button>
    </view>
</view>